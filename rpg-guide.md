# 신라 1000년 RPG — 오픈소스 스타터 코드 설명서

수업에서 학생들이 직접 고치고 확장할 수 있도록 만든 탑다운 RPG 코드입니다.
파일 하나(`rpg-starter.html`)만 있으면 되고, 설치할 것도 결제할 것도 없습니다.

---

## 1. 왜 이 조합인가 — 오픈소스 엔진 비교

수업에서 쓰려면 ①무료·오픈소스일 것 ②설치가 필요 없을 것 ③학생 폰에서도 돌아갈 것
④코드가 한 파일로 읽힐 것, 이 네 가지가 중요합니다. 그 기준으로 비교했습니다.

| 엔진 | 라이선스 | 설치 | 수업 적합성 | 판단 |
|---|---|---|---|---|
| **Phaser 3** | MIT | CDN 한 줄 | 2D 게임 전용, 예제가 매우 많고 한 파일로 완결 가능 | **채택** |
| Godot 4 | MIT | 프로그램 설치 필요 | 강력하지만 내보내기·배포 단계가 중학생에게 부담 | 동아리·장기 프로젝트용 |
| RPG Maker MZ | 상용(유료) | 설치·구매 | 코딩 없이 제작 가능하나 유료이고 코드 학습은 안 됨 | 예산 있으면 대안 |
| Unity | 무료(비오픈소스) | 대용량 설치 | 3D까지 되지만 수업 4시간에는 과함 | 부적합 |
| ct.js | MIT | 설치 필요 | 블록+코드 병행이 장점 | 초등·저학년 대안 |
| 바닐라 JS + Canvas | — | 없음 | 가장 가볍지만 충돌·카메라를 전부 직접 구현 | 상급자 과제용 |

> **결론: Phaser 3.** MIT 라이선스라 수업·배포에 제약이 없고,
> `<script src="...phaser.min.js">` 한 줄이면 끝나며, 학생 휴대전화 브라우저에서 그대로 돕니다.

---

## 2. 실행 방법

1. `rpg-starter.html` 파일을 더블클릭 → 브라우저에서 바로 열립니다.
2. 인터넷이 필요합니다(Phaser를 CDN에서 불러오므로). 인터넷이 불안정한 교실이라면
   [phaser.min.js](https://cdnjs.cloudflare.com/ajax/libs/phaser/3.80.1/phaser.min.js)를
   미리 내려받아 같은 폴더에 두고, HTML의 script 주소를 `phaser.min.js`로 바꾸면 오프라인에서도 됩니다.
3. 조작: **방향키 이동 / Space 말걸기**. 휴대전화는 화면의 방향 버튼과 `말걸기` 버튼.

---

## 3. 코드 고치는 법 — 네 군데만 보면 됩니다

코드 안에 `[고치기 1]` ~ `[고치기 4]` 표시가 달려 있습니다. 그 아래만 손대면 됩니다.

### [고치기 1] 지도 만들기

글자 하나가 32픽셀 한 칸입니다.

```js
const MAP = [
"########################",
"#....T......#.....T....#",
"#.......@..............#",   // @ = 주인공 시작 위치
"########################"];
```

| 글자 | 뜻 | 지나갈 수 있나 |
|---|---|---|
| `.` | 길 | O |
| `#` | 담장 | X |
| `~` | 물(월지) | X |
| `T` | 나무 | X |
| `B` | 건물 | X |
| `@` | 주인공 시작 칸 | O |

**주의:** 모든 줄의 글자 수가 같아야 합니다. 그리고 유물이나 인물을 담장으로 둘러싸면
영영 닿을 수 없으니, 반드시 길(`.`)로 이어진 자리에 두세요.

### [고치기 2] 인물 추가하기

```js
{id:"kim", name:"김유신", x:12, y:6, color:0x88aaff,
 lines:["나는 화랑 출신 장군이다.", "삼국 통일의 길은 멀었다."],
 quiz:0,                 // 대화가 끝나면 QUIZ[0]번 문제를 낸다 (없으면 이 줄 삭제)
 quest:"김유신과 대화"}    // 퀘스트 이름
```

`x`, `y`는 칸 좌표(0부터 셉니다). `color`는 16진수 색(`0xRRGGBB`).

### [고치기 3] 유물 추가하기

```js
{id:"bell", name:"성덕대왕신종", x:18, y:3, color:0xa9b7c0,
 desc:"구리 수만 근을 들여 만든 거대한 종."}
```

주인공이 닿으면 자동으로 획득되고, 설명이 화면 아래에 뜹니다.

### [고치기 4] 확인 문제 추가하기

```js
{q:"무구정광대다라니경이 나온 탑은?",
 o:["석가탑","다보탑","미륵사지석탑","분황사탑"],
 a:0,                                    // 정답 번호 (0부터)
 e:"불국사 석가탑에서 나왔습니다."}         // 정답 뒤 설명
```

---

## 4. 수업 운영 제안 (2차시 구성)

| 단계 | 시간 | 활동 |
|---|---|---|
| 도입 | 10분 | 완성본을 먼저 플레이해 보게 한다. "이걸 우리가 고칠 거야" |
| 실습 1 | 15분 | `[고치기 1]` 지도 바꾸기 — 우리 반만의 왕경 그리기 |
| 실습 2 | 20분 | `[고치기 2~3]` 인물과 유물 추가 — 모둠별로 담당 유산 1개씩 |
| 실습 3 | 20분 | `[고치기 4]` 확인 문제 만들기 — 친구가 풀 문제를 낸다 |
| 공유 | 15분 | 모둠별 작품 플레이 및 상호 평가 |

**모둠 편성 팁:** 지도 담당 1명, 인물·대사 담당 2명, 문제 담당 1명으로 나누면
코딩에 자신 없는 학생도 역할이 생깁니다. 대사와 문제를 쓰는 일이 사실상
역사 조사 활동이라, 코딩보다 내용 학습 시간이 더 길어집니다.

**자주 나는 오류 세 가지**
1. 지도 줄마다 글자 수가 다름 → 화면이 깨집니다. 글자 수를 맞추세요.
2. 항목 사이 쉼표(`,`)를 빠뜨림 → 검은 화면. 브라우저에서 F12 → Console에 오류가 뜹니다.
3. 따옴표를 한쪽만 닫음 → 같은 증상. 색이 이상하게 보이면 따옴표를 확인하세요.

---

## 5. 더 해 볼 만한 확장 과제

- **난이도 상**: 인물마다 다른 표정 텍스처를 만들어 대사에 따라 바꾸기
- **난이도 중**: 유물을 모두 모으면 엔딩 화면이 뜨도록 만들기
- **난이도 하**: 타일 색을 바꿔 계절(봄 경주 / 가을 경주) 지도 만들기
- **연계**: 도보여행에서 찍은 사진을 텍스처로 불러와 실제 경주 지도로 바꾸기

---

## 6. 라이선스와 출처

- Phaser 3 — MIT License (https://phaser.io)
- 이 스타터 코드 — 수업용으로 자유롭게 복제·수정·배포하셔도 됩니다.
- 그래픽은 모두 코드로 그려지므로 별도의 이미지 저작권 문제가 없습니다.
  실제 사진을 넣으실 경우에는 공공누리 개방자료나 직접 촬영본을 사용하세요.
