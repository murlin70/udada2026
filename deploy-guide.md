# 학생 휴대전화로 게임 배포하기 — 안내서

두 게임은 **설치가 필요 없는 웹 게임**입니다. 학생은 링크만 열면 됩니다.
문제는 "그 링크를 어떻게 만들고 나눠 주느냐"뿐입니다. 네 가지 방법을 상황별로 정리했습니다.

---

## 먼저 알아 둘 것

| 항목 | 내용 |
|---|---|
| 앱 설치 | 필요 없음 (브라우저에서 바로 실행) |
| 로그인·가입 | 필요 없음 |
| 데이터 사용량 | 첫 접속 약 150KB. 이후에는 거의 0 (사진 1장보다 적음) |
| 소리 | 게임 1·2 모두 배경음악(국악풍, 코드로 합성). 시작 화면에서 **소리 끄고 시작** 선택 가능, 진행 중에도 소리 버튼으로 끄고 켤 수 있음 |
| 저장 | 각자 휴대전화 안에만 저장. 교사가 원격으로 볼 수 없음 |
| 제출 | **결과 화면 캡처**로 제출받는 것이 가장 확실 |
| 기종 | 안드로이드 크롬 / 아이폰 사파리 모두 동작 |

---

## 방법 1 — Claude 아티팩트 링크 (가장 빠름, 5분)

이미 게시해 둔 링크가 있습니다. 링크를 가진 사람은 **Claude 계정 없이** 열 수 있습니다.

- 게임 1 경주 왕경 탐험 : `https://claude.ai/artifact/HEZajGmmDj7PttT5YLUsw4`
- 게임 2 천년의 문 : `https://claude.ai/artifact/HBfQiaKhBsrv5wP6wgNsVV`

(`assets/qr-game1.png`, `assets/qr-game2.png`가 이 두 주소의 QR입니다.)

**반드시 먼저 확인하세요.** 선생님 계정이 Team·Enterprise(학교·기관 단체 계정)라면
아티팩트가 조직 내부로만 공유되어 학생이 열지 못합니다.

> **확인 방법**: 선생님 휴대전화에서 **시크릿 모드(프라이빗 브라우징)** 로 위 링크를 엽니다.
> 로그인 화면이 뜨면 → 방법 2로 가세요. 게임이 바로 뜨면 → 그대로 쓰셔도 됩니다.

이 방법은 링크가 길고 외우기 어려우니 QR 코드로 나눠 주세요(첨부된 QR 이미지 2장).

---

## 방법 2 — GitHub Pages (권장, 한 번 만들면 계속 사용)

무료이고, 주소가 깔끔하고, 한 번 올려 두면 내년에도 그대로 씁니다. 10분이면 됩니다.

1. [github.com](https://github.com) 가입 (무료)
2. 오른쪽 위 `+` → **New repository**
   - Repository name: `gyeongju` (원하는 이름)
   - **Public** 선택 → Create repository
3. `uploading an existing file` 클릭 → 저장소 폴더의 **내용물을 통째로 드래그**
   - `index.html`, `game1.html`, `game2.html`, `rpg.html`, `README.md`, `.nojekyll`
   - `docs` 폴더와 `assets` 폴더도 함께
   - 아래 **Commit changes** 클릭
4. 상단 **Settings** → 왼쪽 **Pages**
   - Branch: `main` / `(root)` 선택 → **Save**
5. 1~2분 뒤 주소가 나옵니다:
   `https://<아이디>.github.io/gyeongju/`

이 주소 하나면 학생이 **게임 선택 화면**(index.html)으로 들어와 원하는 게임을 고릅니다.
QR도 이 주소 하나만 만들면 됩니다.

---

## 방법 3 — Netlify Drop (가입만 하면 드래그 한 번)

깃허브가 낯설면 이쪽이 더 쉽습니다.

1. [app.netlify.com/drop](https://app.netlify.com/drop) 접속
2. **저장소 폴더 자체를 창 안으로 드래그**
3. 몇 초 뒤 `https://랜덤이름.netlify.app` 주소가 생성됩니다
4. Site settings → Change site name 에서 `gyeongju-silla` 처럼 바꾸면 주소가 짧아집니다

주소를 계속 유지하려면 무료 가입이 필요합니다.

---

## 방법 4 — 인터넷 없이, 교실 안에서만 (가장 안전한 대비책)

학교 와이파이는 되는데 외부 인터넷이 막혀 있거나 느린 경우에 씁니다.
**교사 노트북이 잠깐 서버가 되는 방식**입니다.

1. 노트북과 학생 휴대전화를 **같은 와이파이**에 연결
2. 파일들이 있는 폴더에서 터미널(명령 프롬프트)을 열고 실행

   - 윈도우: `py -m http.server 8000`
   - 맥: `python3 -m http.server 8000`

3. 노트북의 내부 IP 주소 확인
   - 윈도우: `ipconfig` → IPv4 주소 (예: 192.168.0.15)
   - 맥: 시스템 설정 → 네트워크 → 세부사항 → IP 주소
4. 학생에게 알려 줄 주소: `http://192.168.0.15:8000`

**오프라인 대비 두 가지**
- 글꼴은 인터넷에서 불러오지만, 못 불러와도 기본 글꼴로 정상 작동합니다(모양만 조금 달라짐).
- `rpg.html`은 Phaser를 인터넷에서 불러오므로 오프라인에서는 실행되지 않습니다.
  필요하면 [phaser.min.js](https://cdnjs.cloudflare.com/ajax/libs/phaser/3.80.1/phaser.min.js)를
  미리 받아 같은 폴더에 넣고, `rpg.html` 안의 script 주소를 `phaser.min.js`로 바꾸세요.
  (게임 1·2는 라이브러리가 없어 오프라인에서도 그대로 돌아갑니다.)

---

## 이렇게는 하지 마세요

| 방법 | 왜 안 되나 |
|---|---|
| 구글 드라이브에 HTML 올려 공유 | 드라이브는 HTML을 **실행하지 않고 코드로만** 보여 줍니다 |
| 카카오톡·메일로 파일 전송 | 아이폰은 받은 HTML 파일을 브라우저로 열기가 매우 번거롭습니다 |
| 구글 사이트에 코드 삽입 | 삽입 창 크기 제한이 있어 게임 화면이 잘립니다 |
| 클래스룸에 파일 첨부 | 같은 이유. **파일이 아니라 링크**를 올리세요 |

정리하면 — **파일을 나눠 주지 말고, 주소를 나눠 주세요.**

---

## QR 코드 만들기

첨부된 QR 이미지 2장은 방법 1(아티팩트 링크)용입니다.
방법 2·3으로 새 주소를 만드셨다면 QR도 새로 만들어야 합니다.

- 네이버·구글에서 `QR 코드 만들기` 검색 → 주소 붙여넣기 → 다운로드
- 만든 QR을 **PPT 한 장에 크게 띄워** 두면 학생들이 각자 카메라로 찍습니다
- 활동지 ②의 `주소 / QR 코드 부착란`에 인쇄해 붙여도 됩니다
- QR을 못 찍는 학생을 위해 **주소도 칠판에 함께** 적어 주세요

---

## 수업 당일 체크리스트

**전날**
- [ ] 선생님 휴대전화 시크릿 모드로 링크 접속 테스트
- [ ] 다른 기종(안드로이드 + 아이폰) 각 1대씩 추가 테스트
- [ ] 두 게임을 끝까지 한 번 플레이해 보기 (소요 시간 체감용)
- [ ] QR을 띄울 PPT 한 장 준비, 주소 칠판 판서용으로 메모

**수업 시작 5분**
- [ ] 와이파이 비밀번호 먼저 공유
- [ ] QR 띄우고 전원 접속 확인 → 못 들어온 학생은 옆자리와 함께 보기
- [ ] "휴대전화는 이 시간에만 사용, 끝나면 바로 넣기" 규칙 합의
- [ ] 게임 1은 **가로 화면**이 편하다고 안내 (세로로도 됩니다)
- [ ] 교실이 시끄러워지지 않게 "소리 끄고 시작" 또는 이어폰 사용을 미리 정해 두기

**진행 중**
- [ ] 게임 2는 중간에 나가도 이어서 되니, 서두르지 않게 안내
- [ ] 진도가 빠른 학생 → 게임 1로 놓친 유물 마저 모으기
- [ ] 진도가 느린 학생 → 게임 2 메뉴에서 글자 속도를 '빠르게'로

**마무리**
- [ ] 결과 화면 캡처 제출 (패들렛·클래스룸·단톡방 중 편한 곳)
- [ ] 활동지 ②에 점수·등급 기록

---

## 첨부 파일 구성

```
gyeongju-silla/
 ├─ index.html          ← 게임 선택 화면 (이 주소 하나만 나눠 주면 됩니다)
 ├─ game1.html          ← 경주 왕경 탐험 (횡스크롤)
 ├─ game2.html          ← 천년의 문 (비주얼노벨)
 ├─ rpg.html            ← RPG 스타터 (학생 제작 실습용)
 ├─ README.md           ← 깃허브 첫 화면 설명
 ├─ .nojekyll           ← 깃허브 페이지 처리용 빈 파일 (지우지 마세요)
 ├─ docs/
 │   ├─ deploy-guide.md ← 이 문서
 │   └─ rpg-guide.md    ← RPG 수정 가이드
 └─ assets/
     ├─ qr-game1.png
     └─ qr-game2.png
```

네 개의 html 파일은 **같은 폴더에 함께** 두어야 선택 화면의 링크가 작동합니다.
